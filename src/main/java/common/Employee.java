package common;

public class Employee extends Account {

    public Employee(int idAccount, String role, String name,
                    String username, String phoneNumber, String password) {
        super(idAccount, role, name, username, phoneNumber, password);
    }
}
